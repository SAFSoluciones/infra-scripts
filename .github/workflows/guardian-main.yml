name: Guardián de Main

on:
  pull_request:          # <--- ESTA LÍNEA ES LA QUE GITHUB DICE QUE FALTA
    branches: [ "main" ]
  workflow_call:         # <--- Esta es la que ya tenías

jobs:
  verificar-permisos:
    runs-on: ubuntu-latest
    steps:
      - name: Validar usuario
        uses: actions/github-script@v6
        with:
          script: |
            // LISTA DE USUARIOS PERMITIDOS
            const allowedUsers = ['psaf-5']; 
            const actor = context.actor;

            console.log("Usuario actual: " + actor);

            if (!allowedUsers.includes(actor)) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: "🛑 **ACCIÓN BLOQUEADA** 🛑\n\nNo tienes permisos para ir a main."
              });
              core.setFailed(`Usuario ${actor} no autorizado.`);
            }
